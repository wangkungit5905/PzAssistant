首要实现的功能：
1、
2、名称和科目配置界面，提供快速模糊定位，
   即用户输入几个汉字后，以输入的词开始的名称或科目将定位到列表头部或中间
3、科目配置页面，无配置一级科目权限的客户登录后，无法通过双击一级科目而开始编辑这个正确，
    但此时应该一级科目旁的编辑按钮也不可用（现在是可用使用，这个就保护漏洞了）
    经查，编辑二级科目是否为一级科目的默认科目时，需要使用编辑一级科目的权限，
    因此，这样的包含还有待进一步考虑
4、
5、外部工具的配置信息是不是应该放置在基本库中，而不应该是账户中
6、
7、
8、
9、

///////////////////////////////////////////////////////////////////
一、一般性功能提升
1、分录表的行修改标记指示
2、各种配置功能
（1）本期统计可配置项
显示：各种行的颜色（主目、子目、总计）、是否粗体、字体大小
打印：（主目、子目、总计），是否粗体、字体大小

（2）明细账
显示：各种行的颜色（分项、月小计、年合计）、是否粗体、字体大小

3、凭证编辑窗口，当应收应付发生变化时，实时显示该子目的余额。这个最好在应用行为设置中有一个启用开关。

4、提供复位当前账户到本站编辑（仅超级用户拥有的权限）如果账户的转入地不是当时指定的转出目的工作站
但实际需要在本站编辑，如果手动修改账户状态，需要更改账户数据库内的转移条目和本地账户缓存条目内的
转移记录，容易出错。这样的一键式启用很便利。但该账户在其他地方的转移状态必须与当前的相符，即在其他地方的
该账户要么删除重新转移，要么改变为其他不可编辑之态（比如改为转入目的与转入站不一致以使账户只读）

二、重大功能
1、通过局域网转移账户。

2、是否可以在基本库中，缓存名称条目信息以便使它们在多个账户中可以使用相同的名称条目拷贝，
减少用户在新建名称条目时的输入动作。
要实现这样的功能，可能必须在每次打开账户时，或自动或手动调用名称条目的更新操作（将来自打开账户中的
名称条目逐个与本地缓存中的比较，如果是新的，则提示用户追加到本地缓存，否则忽略）为减少名称条目的更
新操作的时间开销，可以在本地缓存表中添加字段，该字段是一个文本数字列表，每两个为一组，第一个表示引
用该名称条目的账户代码，第二个参数表示该名称条目在引用账户内的名称条目id。

3、在转换转移时，除附带站点信息外，还可以附带用户信息等在站点间属于全局共享的信息。用户信息的附带只有
以超级用户角色执行账户转移操作时才能附带。

4、有关名称变动的问题，如果一个名称在使用一段时间后改为其他名称，为了能够在正确的时间显示其正确的名称，
有必要设计一个机制来保存名称的时间属性。名称对象要能够根据引用他的对象（二级科目对象）所处的时间属性
来正确判定自己要显示那个名称合适。

三、远期功能需求：
1、计算器
2、普通电子表格
3、

//////////////////////////////////////////////////////////////////////////////
历史记录：


暂停的任务：
3、固定资产管理界面
    如果固定资产已经开始折旧，则不能更改固定资产的某些信息（比如购买日期、买入价、残值、折旧年限、
    资产类别和科目类别等，更不能删除该固定资产）

2、分页打印文档的功能（始于2011-11-20）
    有待改进的缺陷，打印模板的表格视图的边框的去除。
    明细帐的打印分页是针对某个明细科目（甚至还要考虑特定的币种），因此，如果在视图中指定了所有明细科目或所有
    货币类型时，还要考虑这些特殊情况（这时，因为屏幕视图中显示的是所有明细科目或所有币种的发生列表，
    在这种情况下，必须遍历所有明细科目，按每个明细科目进行打印的分页处理），现阶段，如果不能处理这种情况，必须
    在视图没有指定某个明细科目或某个币种时，禁用打印功能。

1、当当前凭证集状态改变时，通知相关子窗口（比如本期统计子窗口，需要更新凭证集状态，但如果是凭证状态从审核态
变为记账态或反之，必须重新进行计算汇总，以便及时更新）；
    实现思路：
        （1）更新凭证集状态：在MainWindow::refreshShowPzsState()方法中，调用相关子窗口到对应方法。
        （2）对于要影响计算结果的凭证状态改变，需要在主窗口类定义一个槽，在pzdialog2类内定义一个信号，
        在pzdialog2.save()方法内触发此信号。（这个信号有别于在浏览凭证时触发的凭证状态改变信号）



